namespace MauiAppHotel
{
    // Declaração parcial da classe AppShell que herda de Shell
    public partial class AppShell : Shell
    {
        // Construtor da classe AppShell
        public AppShell()
        {
            // Inicializa os componentes da página
            InitializeComponent();
        }
    }
}
